{
  "USERS": {
    "DB": {
      "description": "Database user",
      "default_user": "geo"
    },
    "SM_ADMIN": {
      "description": "Subscription Manager admin user",
      "default_user": "swim-sm-admin"
    },
    "GEOFENCING_SERVER_SM": {
      "description": "SWIM ADSB user for Subscription Manager",
      "default_user": "geofencing-service"
    },
    "GEOFENCING_CLIENT_SM": {
      "description": "SWIM Explorer user for Subscription Manager",
      "default_user": "geofencing-viewer"
    },
    "BROKER_ADMIN": {
      "description": "Broker admin user",
      "default_user": "swim-broker-admin"
    },
    "BROKER_MGMT": {
      "description": "Broker Management user",
      "default_user": "swim-broker-mgmt"
    },
    "GEOFENCING_CLIENT_BROKER": {
      "description": "SWIM Explorer user for the broker",
      "default_user": "geofencing-viewer-broker"
    }
  }
}
